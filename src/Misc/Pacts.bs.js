// Generated by BUCKLESCRIPT, PLEASE EDIT WITH CARE


function isDomainValid(pact) {
  var domain = pact.domain;
  if (domain.TAG) {
    return domain._0.length !== 0;
  } else {
    return domain._0 > 0;
  }
}

function isNameValid(pact) {
  if (pact.category === 2) {
    return true;
  } else {
    return pact.name.length !== 0;
  }
}

function isArchdemonicDomain(pact) {
  var domain = pact.domain;
  if (domain.TAG) {
    return false;
  } else {
    return domain._0 < 13;
  }
}

function isFreeDemonDomain(pact) {
  var domain = pact.domain;
  if (domain.TAG) {
    return false;
  } else {
    return domain._0 >= 13;
  }
}

function isPactFromStateValid(pact) {
  if (isDomainValid(pact) && isNameValid(pact)) {
    if (pact.category === 1) {
      return true;
    } else if (pact.category === 2) {
      if (isArchdemonicDomain(pact) && pact.type === 1) {
        return true;
      } else if (isFreeDemonDomain(pact)) {
        return pact.type === 2;
      } else {
        return false;
      }
    } else {
      return false;
    }
  } else {
    return false;
  }
}

export {
  isPactFromStateValid ,
  
}
/* No side effect */
