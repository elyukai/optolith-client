/**
 * &ndash;
 *
 * The en dash.
 */
let ndash = {js|\u2013|js};

/**
 * &minus;
 *
 * The minus sign.
 */
let minus = {js|\u2212|js};

/**
 * \u2060
 *
 * Non-breaking zero-width space.
 */
let nobr = {js|\u2060|js};

/**
 * &nbsp;
 *
 * Non-breaking space.
 */
let nbsp = {js|\u00A0|js};

/**
 * &plusmn;
 *
 * Plus/minus sign.
 */
let plusmn = {js|\u00B1|js};
