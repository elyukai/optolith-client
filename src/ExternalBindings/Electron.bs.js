// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Electron from "electron";

var partial_arg = Electron.app;

function on(param) {
  partial_arg.on((function () {
            switch (param.NAME) {
              case "ready" :
                  return "ready";
              case "windowAllClosed" :
                  return "window-all-closed";
              
            }
          })(), param.VAL);
  
}

var App = {
  on: on
};

function send(payload, $$window) {
  $$window.webContents.send((function () {
            switch (payload.NAME) {
              case "updateNotAvailable" :
                  return "update-not-available";
              case "updateAvailable" :
                  return "update-available";
              case "downloadProgress" :
                  return "download-progress";
              case "autoUpdaterError" :
                  return "auto-updater-error";
              
            }
          })(), payload.VAL);
  
}

var WebContents = {
  send: send
};

var BrowserWindow = {
  WebContents: WebContents
};

var partial_arg$1 = Electron.ipcMain;

function addListener(param) {
  partial_arg$1.addListener((function () {
            switch (param.NAME) {
              case "loadingDone" :
                  return "loading-done";
              case "downloadUpdate" :
                  return "download-update";
              case "checkForUpdates" :
                  return "check-for-updates";
              
            }
          })(), param.VAL);
  
}

var IpcMain = {
  addListener: addListener
};

export {
  App ,
  BrowserWindow ,
  IpcMain ,
  
}
/* partial_arg Not a pure module */
